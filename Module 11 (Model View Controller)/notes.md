# MVC Notes

- Model: stores data and validation.
- View: shows HTML to the user.
- Controller: gets the request, talks to the model, and sends the view.
- Route: connects a URL to a controller function.
- This module is a very small example with only list and create user pages.